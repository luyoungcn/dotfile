# Local secrets and editor/OS artifacts.
secrets.*
*.secret
# Machine-specific overrides (e.g. ~/.config/fish/fish.local) are never committed.
*.local
.DS_Store
*~

# tmux client debug logs generated during local sessions.
tmux-client-*.log
