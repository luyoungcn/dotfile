# The zsh home entry point is managed by install.sh; keep any unrelated local
# copy out of the repository if one is created at the repository root.
/.zshrc

# Local secrets and editor/OS artifacts.
secrets.*
*.secret
.DS_Store
*~

# tmux client debug logs generated during local sessions.
tmux-client-*.log
