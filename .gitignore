# zsh is intentionally outside the scope of this repository for now.
/.zshrc

# Local secrets and editor/OS artifacts.
secrets.*
*.secret
.DS_Store
*~

# tmux client debug logs generated during local sessions.
tmux-client-*.log
